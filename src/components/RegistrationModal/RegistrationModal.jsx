import React from 'react';
import styles from './RegistrationModal.module.scss'

const RegistrationModal = ({isLogin,setIsLogin}) => {
    return (
        <div>
            register
        </div>
    );
};

export default RegistrationModal;